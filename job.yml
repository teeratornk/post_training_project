$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json

command: >-
  python launch_temperature_sweep.py

environment: azureml:tkad15-grpo@latest
compute: azureml:tkad15-8-v100-sc
experiment_name: temp17
resources:
  instance_count: 2

environment_variables:
  PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"

# distribution:
#   type: pytorch
#   process_count_per_instance: 4
# accelerate launch --config_file acc_config.yaml hf_grpo_example1_accelerate.py
code: .
